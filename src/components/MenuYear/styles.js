import styled from 'styled-components';
import DropdownButtonBootstrap from 'react-bootstrap/DropdownButton';

export const DropdownButton = styled(DropdownButtonBootstrap)`
    .dropdown-menu {
        min-width: 1em;
    }
    #dropdown-basic-button {
        background: #0366d6;
    }
`;
