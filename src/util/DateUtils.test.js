/* eslint-disable */
import { convertMonth } from './DateUtils';

describe('Test Array Utils', () => {
    test('should have return month in string', () => {
        expect(convertMonth[1]).toEqual('Jan');
    });
});
